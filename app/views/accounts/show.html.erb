<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @account.user_id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @account.name %>
</p>

<td><%= link_to 'Invite', account_invite_path(@account)  %></td>
<%= link_to 'Edit', edit_account_path(@account) %> |
<%= link_to 'Back', accounts_path %>
<%= link_to 'Go to Boxes', boxes_path %>
